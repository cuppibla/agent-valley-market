# How this lab talks to Gemini. Pick ONE — the first is the one the codelab uses.

# ── 1. Vertex AI (default) ────────────────────────────────────────────────────
# Your own Google Cloud project. Nothing to paste, nothing to leak. In Cloud Shell
# you are already signed in, so this is all it takes:
#
#   gcloud auth application-default login     # not needed in Cloud Shell
#   gcloud config set project YOUR_PROJECT_ID
#
# Leave the line below as-is and the lab picks up that project on its own.
GOOGLE_GENAI_USE_VERTEXAI=TRUE
# GOOGLE_CLOUD_PROJECT=your-project-id      # only if gcloud isn't already set
# GOOGLE_CLOUD_LOCATION=global

# ── 2. Or an API key, if you'd rather not use a project ───────────────────────
# Free one at https://aistudio.google.com/apikey — then comment out the
# GOOGLE_GENAI_USE_VERTEXAI line above and uncomment this one.
# GOOGLE_API_KEY=
